app_name: Scholarship Payment Platform
version: 1.0.0
debug: true

database:
  url: postgresql://postgres:postgres@localhost:5432/scholarship_platform

app:
  secret_key: change-this-in-production-use-strong-random-key
  jwt_secret_key: jwt-secret-key-change-in-production
